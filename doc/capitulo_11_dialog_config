#
# Run-time configuration file for dialog
#
# Automatically generated by "dialog --create-rc <file>"
#
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?,underline?,reverse?)

# Set aspect-ration.
aspect = 0

# Set separator (for multiple widgets output).
separate_widget = ""

# Set tab-length (for textbox tab-conversion).
tab_len = 0

# Make tab-traversal for checklist, etc., include the list.
visit_items = OFF

# Shadow dialog boxes? This also turns on color.
use_shadow = OFF

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (GREEN,BLACK,ON)

# Shadow color
shadow_color = (BLACK,BLACK,ON)

# Dialog box color
dialog_color = (BLACK,GREEN,OFF)

# Dialog box title color
title_color = dialog_color

# Dialog box border color
border_color = dialog_color

# Active button color
button_active_color = dialog_color

# Inactive button color
button_inactive_color = dialog_color

# Active button key color
button_key_active_color = (GREEN,BLACK,OFF)

# Inactive button key color
button_key_inactive_color = dialog_color

# Active button label color
button_label_active_color = button_key_active_color

# Inactive button label color
button_label_inactive_color = dialog_color

# Input box color
inputbox_color = screen_color

# Input box border color
inputbox_border_color = screen_color

# Search box color
searchbox_color = screen_color

# Search box title color
searchbox_title_color = button_key_active_color

# Search box border color
searchbox_border_color = button_key_active_color

# File position indicator color
position_indicator_color = dialog_color

# Menu box color
menubox_color = button_key_active_color

# Menu box border color
menubox_border_color = button_key_active_color

# Item color
item_color = button_key_active_color

# Selected item color
item_selected_color = dialog_color

# Tag color
tag_color = button_key_active_color

# Selected tag color
tag_selected_color = dialog_color

# Tag key color
tag_key_color = button_key_active_color

# Selected tag key color
tag_key_selected_color = dialog_color

# Check box color
check_color = button_key_active_color

# Selected check box color
check_selected_color = dialog_color

# Up arrow color
uarrow_color = screen_color

# Down arrow color
darrow_color = screen_color

# Item help-text color
itemhelp_color = screen_color

# Active form text color
form_active_text_color = (WHITE,BLUE,ON)

# Form text color
form_text_color = (WHITE,CYAN,ON)

# Readonly form item color
form_item_readonly_color = (CYAN,WHITE,ON)

# Dialog box gauge color
gauge_color = (BLUE,WHITE,ON)

# Dialog box border2 color
border2_color = (BLACK,WHITE,OFF)

# Input box border2 color
inputbox_border2_color = border2_color

# Search box border2 color
searchbox_border2_color = border2_color

# Menu box border2 color
menubox_border2_color = border2_color
